console.log("FreshVeg Website Loaded");

// Shop buttons

const buttons = document.querySelectorAll("button");

buttons.forEach(button => {
    button.addEventListener("click", () => {
        alert("Feature will be connected to backend soon!");
    });
});